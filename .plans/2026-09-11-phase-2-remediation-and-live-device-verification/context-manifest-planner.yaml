role: planner
plan: 2026-09-11-phase-2-remediation-and-live-device-verification
generated_at: 2026-09-11T16:50:04Z
request: "Phase 2 remediation and live-device verification for the existing AMICO SDK implementation. The C++ SDK (AmicoClient::login, isSessionValid, getSystemInformation, users().list/get, accessLogs().list, logout) already exists as candidate WIP from the blocked P5 plan (.plans/2026-09-11-p5-rewrite-the-phase-2-read-only-c-17-am). Offline build/tests previously passed (38 cases/171 assertions) and secret scan passed, but under same-context self-verification that must NOT be treated as independent. Spec must cover: (A) governance remediation - preserve existing implementation, no rewrite merely for provenance, fresh execution approval after Plan Reviewer PASS, never reuse/infer approval; (B) independent offline verification - fresh Verifier context, clean build dir, reconfigure+rebuild from scratch, rerun full offline suite + secret scan, record compiler/CMake/dependency versions, no reuse of old Executor build/binaries; (C) live-device smoke verification against 192.168.2.156, read-only only (no create/update/delete of users/cards/identities/access rules/config/settings/permissions/credentials/logs), covering: network reachability/preflight, HTTPS/TLS behavior, auth/login, session cookie acquisition, session validity, getSystemInformation(), read-only user listing, users().get(id) only with a known-safe id, read-only access-log listing, logout/session cleanup; (D) credential handling - no secrets in source/spec/tasks/tests/DECISION_LOG/git, live creds only via approved env vars/secret mechanism at execution time, never print password or session token; (E) live-test gate - running the live smoke test requires its own separate explicit approval token APPROVE_LIVE_DEVICE_TEST:<plan-id>, never inferred from APPROVE_SPEC_ONLY/APPROVE_EXECUTION/OK/proceed; (F) failure handling - if live verification finds a genuine defect, stop, record evidence, no silent patch in Verifier role, route back through Harness to Executor for any source fix, then re-verify from a fresh clean Verifier context; (G) actor/context separation for Planner/Plan Reviewer/Executor(if needed)/Verifier - do not invent Executor work if no source change is needed; (H) completion criteria - valid approval provenance, independent clean offline verification pass, live-device verification status explicitly recorded, all approved read-only live checks pass, no credential leakage, any defects routed through proper Executor -> fresh Verifier flow. This Planner session must only inspect docs/existing code read-only and write spec.md, then stop at the spec-approval gate. Do not implement, build, test, or contact any device."
project_sections:
skills:
  - karpathy-guidelines: "explicitly enabled"
  - cpp: "matched request text"
  - cmake: "matched request text"
  - testing: "matched request text"
  - linux: "matched request text"
tools:
